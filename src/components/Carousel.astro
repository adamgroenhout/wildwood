---
import { Image } from 'astro:assets';

interface Props {
  images: string[];
}

const { images } = Astro.props;
---

<div class="carousel-container relative overflow-hidden rounded-brand border border-current/10 bg-current/5 shadow-lg group">
  <div class="carousel-track flex transition-transform duration-500 ease-in-out">
    {images.map((img, index) => (
      <div class="carousel-slide min-w-full aspect-video flex items-center justify-center">
        {img ? (
          <Image src={img} alt={`Studio image ${index + 1}`} width={800} height={450} class="w-full h-full object-cover" />
        ) : (
          <div class="w-full h-full bg-current/10 flex items-center justify-center opacity-50 italic">
            Placeholder for Image {index + 1}
          </div>
        )}
      </div>
    ))}
  </div>

  <!-- Navigation Arrows -->
  {images.length > 1 && (
    <>
      <button 
        class="prev-btn absolute left-4 top-1/2 -translate-y-1/2 bg-black/50 hover:bg-black/70 text-paper p-3 rounded-full opacity-0 group-hover:opacity-100 transition-opacity z-10"
        aria-label="Previous slide"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
      </button>
      <button 
        class="next-btn absolute right-4 top-1/2 -translate-y-1/2 bg-black/50 hover:bg-black/70 text-paper p-3 rounded-full opacity-0 group-hover:opacity-100 transition-opacity z-10"
        aria-label="Next slide"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
      </button>

      <!-- Dots -->
      <div class="absolute bottom-4 left-1/2 -translate-x-1/2 flex gap-2 z-10">
        {images.map((_, index) => (
          <button 
            class={`carousel-dot w-2 h-2 rounded-full bg-current/30 hover:bg-current/60 transition-colors ${index === 0 ? 'bg-current' : ''}`}
            data-index={index}
            aria-label={`Go to slide ${index + 1}`}
          />
        ))}
      </div>
    </>
  )}
</div>

<script>
  function setupCarousel() {
    const containers = document.querySelectorAll('.carousel-container');
    
    containers.forEach(container => {
      const track = container.querySelector('.carousel-track') as HTMLElement;
      const slides = container.querySelectorAll('.carousel-slide');
      const dots = container.querySelectorAll('.carousel-dot');
      const prevBtn = container.querySelector('.prev-btn');
      const nextBtn = container.querySelector('.next-btn');
      
      let currentIndex = 0;
      const totalSlides = slides.length;

      function updateCarousel() {
        if (!track) return;
        track.style.transform = `translateX(-${currentIndex * 100}%)`;
        dots.forEach((dot, idx) => {
          if (idx === currentIndex) {
            dot.classList.add('bg-current');
            dot.classList.remove('opacity-30');
          } else {
            dot.classList.remove('bg-current');
            dot.classList.add('opacity-30');
          }
        });
      }

      function nextSlide() {
        currentIndex = (currentIndex + 1) % totalSlides;
        updateCarousel();
      }

      function prevSlide() {
        currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
        updateCarousel();
      }

      nextBtn?.addEventListener('click', nextSlide);
      prevBtn?.addEventListener('click', prevSlide);

      dots.forEach(dot => {
        dot.addEventListener('click', () => {
          currentIndex = parseInt(dot.getAttribute('data-index') || '0');
          updateCarousel();
        });
      });
    });
  }

  // Run on initial load and view transitions
  setupCarousel();
  document.addEventListener('astro:after-swap', setupCarousel);
</script>
